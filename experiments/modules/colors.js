export const name = 'colors';

const colorDefinitions = [
    [{
        innerColor: '#4747FF',
        outerColor: '#C2C200'
    }],
    [{
        innerColor: 'blue',
        outerColor: 'gray'
    }],
];

export { colorDefinitions }